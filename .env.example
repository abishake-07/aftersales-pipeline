# ─────────────────────────────────────────────────────────────
# Copy this file to .env and fill in your values.
# .env is gitignored — your values stay local.
# ─────────────────────────────────────────────────────────────

# AWS account ID (12-digit number, NOT a secret but don't publish it)
AWS_ACCOUNT_ID=903452056226

# AWS region for all resources
AWS_REGION=eu-central-1

# AWS CLI profile name (from ~/.aws/credentials)
# Leave as "default" if you only have one profile.
AWS_PROFILE=default

# ─────────────────────────────────────────────────────────────
# ⚠️  NEVER put these in this file:
#     - AWS_ACCESS_KEY_ID
#     - AWS_SECRET_ACCESS_KEY
#     - AWS_SESSION_TOKEN
#
# Instead, configure them via:
#     aws configure --profile default
#
# Or use AWS SSO:
#     aws configure sso
# ─────────────────────────────────────────────────────────────
